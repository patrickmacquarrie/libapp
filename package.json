{
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "test": "npm run test:engine && npm run test:operations && npm run test:rules",
    "test:engine": "node scripts/test-engine-audit.js",
    "test:operations": "node scripts/test-operations-audit.js",
    "test:rules": "CI=true firebase emulators:exec --only firestore,auth \"node scripts/test-firestore-rules.js\"",
    "verify:live-rules": "node scripts/verify-live-firestore-rules.js",
    "check": "npm test && npm run build"
  },
  "devDependencies": {
    "@babel/standalone": "7.23.5",
    "firebase-tools": "15.25.1",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "sharp": "^0.35.3"
  }
}
