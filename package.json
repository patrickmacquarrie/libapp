{
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "test": "npm run test:engine && npm run test:rules",
    "test:engine": "node scripts/test-engine-audit.js",
    "test:rules": "firebase emulators:exec --only firestore,auth \"node scripts/test-firestore-rules.js\"",
    "check": "npm test && npm run build"
  },
  "devDependencies": {
    "@babel/standalone": "7.23.5",
    "firebase-tools": "15.25.1",
    "sharp": "^0.35.3"
  }
}
